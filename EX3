def EX3_5(nums):
    summ=0
    i=0
    x=len(nums)
    for a in nums:
         if i==x:
              break
         elif a<0:
              a=a*(-1)
    summ=summ + nums[i]
    i=i+1
    
        
    return summ
